{
  "scripts": {
    "start": "pulp -w build",
    "build": "rm -rf dist/*; pulp build -O --to public/index.js; parcel build public/index.html --no-source-maps; rm -r public/index.js"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
