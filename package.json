{
  "scripts": {
    "start": "pulp -w build",
    "start:page": "pulp build -O --to public/index.js; parcel public/index.html",
    "build": "rm -rf dist/*; pulp build -O --to public/index.js; parcel build public/index.html --no-source-maps --public-url /band-it/; rm -r public/index.js"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
